# Vermilion Standard Library

Basis for new C applications

(WIP: More added as needed by core project)

# Build

Creates a static library by default

Using CMake and MinGW Make:

mkdir build
cd build
cmake .. -G "MinGW Makefiles"
make
